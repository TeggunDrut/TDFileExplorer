#include "Chunk.h"

void Chunk::Activate() {

  // for(Voxel v : Chunk::voxels) {
  //   if(v.x > Chunk::maxX) {
  //     Chunk::maxX = v.x;
  //   }
  //   if(v.y > Chunk::maxY) {
  //     Chunk::maxY = v.y;
  //   }
  //   if(v.z > Chunk::maxZ) {
  //     Chunk::maxZ = v.z;
  //   }
  // }

}

void Chunk::Draw(Shader &shader, Camera &camera) {
  
}